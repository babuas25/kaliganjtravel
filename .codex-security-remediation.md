# Temporary security remediation memory

Created: 2026-08-01
Branch: `development`

Delete this file only after every code-addressable item below is implemented and
the full verification suite passes.

## High priority

- [x] Bind each repriced flight selection to the authenticated pricing principal
  (user, audience, and agency) and reject mismatches during booking preparation.
- [x] Apply least-privilege booking scope and PII projection: Media Staff own
  bookings only, Accounts Staff financial-only, Support Staff masked passports.
- [ ] Stop using a Clerk development instance on the public production site.
  Create/promote the Clerk production instance, migrate or recreate authorized
  users and role metadata, then rotate Vercel to the production keys without
  locking out the current administrators.

## Medium priority

- [x] Apply strict nonce CSP to booking resume and checkout.
- [x] Stop persisting passenger names/phones in localStorage and clear booking
  capability tokens after successful completion.
- [x] Replace per-instance rate limits with a shared, atomic database limiter;
  key authenticated operations by user and public operations by bounded IP.
- [x] Serialize privileged user role/delete operations so concurrent requests
  cannot remove the last Super Admin.
- [x] Add durable security audit events for privileged account, invitation,
  application-review, agency-user, and sensitive-document operations.
- [x] Implement booking-attempt retention and duplicate passenger-snapshot
  cleanup.

## Low priority

- [x] Generate invitation redirects from a validated canonical application URL,
  never the request Host header.
- [x] Re-read invitation metadata before revocation and enforce the same role
  hierarchy used for account management.

## External controls to verify separately

- [ ] Clerk: require MFA for Super Admin/Admin and review session lifetime,
  breached-password protection, and bot protection.
- [x] Confirm the production database migration is applied before deploying code
  that depends on the new security RPCs.

## Verification checklist

- [x] Targeted security tests
- [x] `npm run lint`
- [x] `npm run typecheck`
- [x] `npm run build`
- [x] `npm audit --omit=dev`
- [ ] Safe unauthenticated production probes after deployment
- [ ] Working tree reviewed for unintended files
